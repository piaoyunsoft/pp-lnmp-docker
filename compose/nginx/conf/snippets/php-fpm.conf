# 复用 PHP-FPM 上游配置 (站点 include 使用)
# 用法: include /etc/nginx/snippets/php-fpm.conf;
# 需站点 server 块内先 set $php_upstream "site_a_php";

location ~ \.php$ {
    try_files $uri =404;
    fastcgi_split_path_info ^(.+\.php)(/.+)$;
    fastcgi_pass            $php_upstream;
    fastcgi_index           index.php;
    fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    include                 fastcgi_params;

    fastcgi_buffer_size          128k;
    fastcgi_buffers           4  256k;
    fastcgi_busy_buffers_size    256k;
    fastcgi_temp_file_write_size 256k;
    fastcgi_read_timeout         600;
    fastcgi_connect_timeout      30;
    fastcgi_send_timeout         600;
    fastcgi_keep_conn on;

    # SSE (text/event-stream) 需要关闭缓冲, 否则浏览器收不到实时数据
    # X-Accel-Buffering 只对 proxy_pass 生效, fastcgi 必须用此指令
    fastcgi_buffering off;
}

# 静态资源缓存
location ~* \.(?:jpg|jpeg|gif|png|ico|svg|webp|avif|woff2?|ttf|eot|css|js|map)$ {
    expires 30d;
    access_log off;
    add_header Cache-Control "public, immutable";
    try_files $uri =404;
}

# 隐藏文件保护
location ~ /\.(?!well-known) { deny all; }
